import 'package:flutter/material.dart';

class SleepStressPage extends StatelessWidget {
  const SleepStressPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Sleep & Stress Page")),
    );
  }
}
