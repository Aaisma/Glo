import 'package:flutter/material.dart';

class SkinTrackerPage extends StatelessWidget {
  const SkinTrackerPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text("Skin Tracker Page")),
    );
  }
}
